# Stateful Fuzzing Campaign Code Creation

## Input

I have this contract setup and configuration data structure at `magic/stateful-fuzzing-spec.json`

## Task

Update Setup.sol to perform stateful testing, by deploying and wiring contracts in a way that is consistent with the spec.

The code must compile and after the deployment all contracts must be wired following the spec.

Preserve storage value functionality if storage variables where already there when you began the task.

Always write up Actors and Assets in their respective managers.